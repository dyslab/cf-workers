# Npm scripts in this worker folder

- `npm start`: run the worker locally (for development)

- `npm run deploy`: deploy the worker to the Cloudflare

- `npm run test`: run the snapshot test (under construction)
